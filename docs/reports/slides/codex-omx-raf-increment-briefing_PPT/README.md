# codex-omx-raf-increment-briefing_PPT

- Canvas format: ppt169
- Created: 20260606

## Directories

- `svg_output/`: raw SVG output
- `svg_final/`: finalized SVG output
- `images/`: presentation assets
- `notes/`: speaker notes
- `templates/`: project templates
- `resources/`: source pointers and provenance; do not copy the canonical Markdown here
- `images/`: ignored binary image assets; `images/README.md` is tracked as a placeholder
- `svg_final/`: ignored generated SVG output; `svg_final/README.md` is tracked as a placeholder
- `exports/`: ignored exported PPTX files; `exports/README.md` is tracked as a placeholder
- `backup/<timestamp>/`: ignored local export backups; `backup/README.md` is tracked as a placeholder

## Source of Truth

- Edit from the canonical Markdown, `notes/total.md`, split `notes/*.md`, and `svg_output/*.svg`.
- Do not edit exported PPTX as the source of truth.
- See `resources/source.md` for the canonical source pointer.
